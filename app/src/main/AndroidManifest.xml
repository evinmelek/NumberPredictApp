<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.evin.melek.numberpredictapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/number_predict_logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/number_predict_logo_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.NumberPredictApp">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".PredictScreenActivity">

        </activity>

        <activity android:name=".ResultActivity">

        </activity>


    </application>

</manifest>